const taskService = {
  createTask: async (title, description, status, priority, deadline, createdBy) => {
    try {
    } catch (error) {
      throw error;
    }
  },
};

export default taskService;
